<h1>All Lists</h1>






<% @lists.each do |list| %>
  <%= link_to list_path(list) %>
    <div class="card">
      <%= list.name %>
    </div>
<% end %>

<%= link_to '➕ List', new_list_path %>

 <%# <li><%= link_to list.name, list_path(list) %> <%#</li> %>
